type Author {
  id: ID!
  firstName: String!
  lastName: String!
}

type Query {
  findAllAuthors(id: ID): [Author]!
  countAuthors: Long!
}

type Mutation {
  newAuthor(firstName: String!, lastName: String!) : Author!
}